__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
bin/
.env
.env.dev
*.log
node_modules/
ui/dist/

# AI workflow (keep private), except the committed prompt archive (process evidence)
specs/*
!specs/prompts/
.cursor/
registry/
.reviews/
prompts/reviews/
docs/design-system/
download_specs.sh
CLAUDE.md

# Local dev
.env.dev
.venv/
*.log
__pycache__/
.env.local
.env.local.*
*.local
!.env.local.example

create_agents.sh
opa.exe
.tmp/

# TypeScript incremental build cache (artifact)
ui/tsconfig.tsbuildinfo

# OS cruft
.DS_Store

# Serena MCP — keep internal AI memory/config private (not for public release)
.serena/

# Codebase-memory MCP knowledge graph (local only)
.mcp-memory/

# Lessons / gotchas log (local only)
.lessons/

# Migration / new-machine setup + kubeconfig — internal, never publish
kube/
NEW-MACHINE-SETUP*.md
docs/NEW-MACHINE-SETUP*.md
migrate-pack.ps1

# Day9 / migration untracked artifacts — internal, not for origin (root-anchored)
/package.json
/package-lock.json
scripts/ui-validate.mjs
docs/IMPLEMENTATION-STATUS.md
docs/mcp-workflow.md
ui/tsconfig.node.tsbuildinfo
webhook/webhook
