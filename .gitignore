__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
bin/
.env
.env.dev
*.log
node_modules/
ui/dist/

# AI-workflow config is TRACKED while the repo is PRIVATE (durability + history):
# CLAUDE.md, .cursor/**, .serena/memories + project.yml, docs/mcp-workflow.md, .mcp-memory.
# RE-REVIEW before going public (see docs/engineering/security-baseline.md).
# Still private: specs (except the prompt archive), registry, reviews, design-system.
specs/*
!specs/prompts/
registry/
.reviews/
prompts/reviews/
docs/design-system/
download_specs.sh

# Local dev
.env.dev
.venv/
*.log
__pycache__/
.env.local
.env.local.*
*.local
!.env.local.example

create_agents.sh
opa.exe
.tmp/

# TypeScript incremental build cache (artifact)
ui/tsconfig.tsbuildinfo

# OS cruft
.DS_Store

# Serena MCP — memories + project.yml are TRACKED (AI-workflow config; repo private).
# .serena/cache/ + .serena/project.local.yml stay ignored via .serena/.gitignore (regenerable / local).

# Codebase-memory MCP knowledge graph — TRACKED for durability (repo private).
# (memory.json is absent in this tree today; the path is un-ignored so it tracks when present.)

# Lessons / gotchas log (local only)
.lessons/

# Migration / new-machine setup + kubeconfig — internal, never publish
kube/
NEW-MACHINE-SETUP*.md
docs/NEW-MACHINE-SETUP*.md
migrate-pack.ps1

# Day9 / migration untracked artifacts — internal, not for origin (root-anchored)
/package.json
/package-lock.json
scripts/ui-validate.mjs
docs/IMPLEMENTATION-STATUS.md
ui/tsconfig.node.tsbuildinfo
webhook/webhook
scripts/fleet-local/fleet-signing-*.pem
