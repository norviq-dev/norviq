claude "DEEP REVIEW of F022-F024 CRDs (NrvqPolicy, NrvqClass, NrvqConfig). Read specs/F022.md for the spec. Read ALL files in crds/ and webhook/controller.go.

You are a senior Kubernetes platform engineer. These CRDs become part of the K8s API. Any bug = broken cluster state, stuck resources, or security bypass.

## 1. CRD SCHEMA VALIDATION
For each CRD (nrvqpolicies, nrvqclasses, nrvqconfigs):
- File exists in crds/?
- apiVersion is apiextensions.k8s.io/v1?
- Group is norviq.io?
- Version is v1alpha1?
- Has required fields marked correctly?
- All string enums validated (enforcementMode: block/audit/escalate)?
- Numeric fields have min/max bounds?
- No field allows arbitrary unvalidated input?
- Has status subresource defined?
- Has additionalPrinterColumns for kubectl get output?
- Has short names (np, nc, nconf)?
- Has categories: [norviq]?
- Scope correct? (NrvqPolicy=Namespaced, NrvqClass=Cluster, NrvqConfig=Cluster)
- Report each: PASS or FAIL with file:line

## 2. CRD CLUSTER SAFETY
- Can a malicious NrvqPolicy CR crash the API server? (check: no unbounded string fields without maxLength)
- Can a user create infinite NrvqPolicy CRs? (no resource quota built in — is this OK?)
- Does NrvqPolicy rego field allow arbitrary code execution? (OPA sandbox — verify)
- Can NrvqClass blockedTools be empty? (should it be required?)
- Can NrvqConfig override global enforcement to 'disabled'? Is that safe?
- Does priority field have proper bounds (0-1000)?
- Can two NrvqPolicies conflict? How is precedence resolved?
- If CRD is deleted while controller is running, what happens?
- If CRD YAML has invalid schema, does kubectl apply reject it cleanly?
- Report each: PASS or FAIL

## 3. EXAMPLE MANIFESTS
- crds/examples/policy-strict-chatbot.yaml — valid against CRD schema?
- crds/examples/policy-moderate-analyst.yaml — valid?
- crds/examples/policy-namespace-baseline.yaml — valid?
- crds/examples/class-customer-support.yaml — valid?
- crds/examples/class-data-analyst.yaml — valid?
- crds/examples/config-default.yaml — valid?
- Do examples cover all 3 targeting modes (agentClass, workload, namespace)?
- Do examples show preset, rules, and custom rego options?
- Report each: PASS or FAIL with file

## 4. GO CONTROLLER (webhook/controller.go)
- Uses in-cluster config (not kubeconfig file)?
- Uses dynamic client (correct for CRDs without generated types)?
- Watches NrvqPolicy resources with informer?
- Handles Add, Update, Delete events?
- Syncs policy to Norviq API on create/update?
- Deletes from API on CRD delete?
- API sync is fire-and-forget (goroutine) — doesn't block informer?
- HTTP client has timeout (≤5s)?
- Proper error handling — sync failure doesn't crash controller?
- Auth token passed to API calls?
- Report each: PASS or FAIL with file:line

## 5. CONTROLLER SAFETY
- Can the controller goroutine leak? (unbounded goroutines from fire-and-forget API calls)
- Is there a semaphore or worker pool limiting concurrent API calls?
- What happens if Norviq API is down? (should retry or log, not crash)
- What happens if CRD watch connection drops? (informer auto-reconnects?)
- Does the controller have graceful shutdown?
- Does the controller run as non-root in its container?
- Can a rapid flood of CRD updates cause OOM? (rate limiting?)
- Report each: PASS or FAIL with file:line

## 6. CONTROLLER CONCURRENCY
- Is the Controller struct safe for concurrent use?
- Any shared maps/slices written without mutex?
- Does handlePolicy modify any global state?
- Could two simultaneous CRD updates for the same policy race?
- Report each: PASS or FAIL

## 7. HELM INTEGRATION
- CRD files in helm/norviq/crds/ directory?
- Helm auto-installs CRDs on helm install?
- CRDs are NOT in templates/ (they belong in crds/)?
- Helm upgrade does NOT delete CRDs (K8s protects them)?
- helm uninstall does NOT delete CRDs (data safety)?
- Report each: PASS or FAIL with file path

## 8. SECURITY
- NrvqPolicy rego field: can a user inject malicious Rego that escapes OPA sandbox?
- NrvqClass allowedTools: validated as safe strings (no injection)?
- NrvqConfig sidecar.image: can a user point to a malicious image?
- Are RBAC rules needed for who can create/modify these CRDs?
- Should there be a ValidatingWebhookConfiguration for CRD validation beyond schema?
- Can a user in namespace A create a policy that affects namespace B?
- NrvqPolicy is Namespaced — user can only create in their namespace?
- NrvqClass is Cluster-scoped — should it require cluster-admin?
- NrvqConfig is Cluster-scoped — should it require cluster-admin?
- Report each: PASS or FAIL

## 9. TESTING
- webhook/controller_test.go exists?
- Tests CRD add event → API sync called?
- Tests CRD update event → API sync called?
- Tests CRD delete event → handled?
- Tests API sync failure → no crash?
- Tests malformed CRD object → no panic?
- Report each: PASS or FAIL with file:line

## 10. STALE CODE
- Zero commented-out code in controller.go?
- Zero TODO/FIXME without tracking?
- Zero unused imports?
- Zero fmt.Println (use slog)?
- controller.go handlePolicy properly constructs HTTP request body?
- No dead code paths?
- Report each: PASS or FAIL with file:line

## 11. ARCHITECTURE FILES
- architecture/F022.class.mmd exists and valid Mermaid?
- architecture/F022.sequence.mmd exists? Shows: CRD apply → controller watch → API sync?
- architecture/F022.deps.mmd exists? Shows: depends on F016, F017, F021?
- registry/F022.md exists with 12 sections?
- Registry Section 12 debug guide has these rows:
  | CRD not showing in kubectl | CRD YAML not applied | kubectl apply -f crds/ |
  | Policy not syncing to API | Controller not running | check controller logs |
  | kubectl apply rejected | Schema validation failed | check field types/enums |
  | Two policies conflict | Priority not set | set priority field (higher wins) |
  | CRD stuck in Terminating | Finalizer blocking | patch finalizers to null |
- Report each: PASS or FAIL

## 12. KUBECTL USABILITY
- kubectl get np -A shows useful columns (Target, Mode, Phase, Blocks-24h, Age)?
- kubectl get nc shows useful columns (Agents, Avg-Trust, Policies, Rate-Limit, Age)?
- kubectl get nconf shows useful columns (Mode, Trust-Threshold, Rate-Limit, Namespaces, Age)?
- Short names work (np, nc, nconf)?
- kubectl describe shows readable output?
- Report each: PASS or FAIL

## SUMMARY
1. Total PASS / FAIL count
2. CRITICAL (will break cluster or allow security bypass)
3. HIGH (will cause problems in production)
4. MEDIUM (should fix before release)
5. LOW (nice to have)

For every FAIL: exact file:line and specific fix with code." --print 2>&1 | tee .reviews/F022.md