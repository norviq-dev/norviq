---
Feature ID:     S001
Feature Name:   Project Scaffold + Dependencies
Component:      Setup
Phase:          MVP
Complexity:     S
Build Order:    #-3
Depends On:     S000
Blocks:         S002, S003
---

## Cursor Instructions
1. Read this spec completely before writing any code
2. Create EXACTLY the files and directories listed below
3. Do NOT create files not listed
4. Write all files to disk, not just chat

## Files
```
pyproject.toml (CREATE)
go.mod (CREATE)
Makefile (CREATE)
.gitignore (CREATE)
README.md (CREATE)
.github/workflows/ci.yml (CREATE)
norviq/__init__.py (CREATE)
norviq/sdk/__init__.py (CREATE)
norviq/sdk/core/__init__.py (CREATE)
norviq/sdk/langchain/__init__.py (CREATE)
norviq/sdk/langgraph/__init__.py (CREATE)
norviq/sdk/client/__init__.py (CREATE)
norviq/engine/__init__.py (CREATE)
norviq/sidecar/__init__.py (CREATE)
norviq/api/__init__.py (CREATE)
norviq/api/routers/__init__.py (CREATE)
norviq/api/db/__init__.py (CREATE)
norviq/api/db/migrations/ (CREATE dir)
norviq/api/db/jobs/ (CREATE dir)
norviq/webhook/ (CREATE dir — Go, no __init__.py)
norviq/cli/ (CREATE dir — Go, no __init__.py)
norviq/cli/cmd/ (CREATE dir)
ui/src/components/ (CREATE dir)
ui/src/services/ (CREATE dir)
ui/src/store/ (CREATE dir)
helm/norviq/templates/ (CREATE dir)
crds/ (CREATE dir)
docs/ (CREATE dir)
architecture/ (CREATE dir — Mermaid .mmd diagrams per feature)
registry/ (CREATE dir — code registry .md per feature)
.cursor/rules/ (CREATE dir)
tests/sdk/ (CREATE dir)
tests/engine/ (CREATE dir)
tests/sidecar/ (CREATE dir)
tests/api/ (CREATE dir)
tests/integration/ (CREATE dir)
scripts/review.sh (CREATE)
LICENSE (CREATE)
```

## pyproject.toml
```toml
[project]
name = "norviq"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic>=2.0",
    "pydantic-settings>=2.0",
    "structlog>=23.0",
    "httpx>=0.24",
    "opentelemetry-api>=1.20",
    "opentelemetry-sdk>=1.20",
    "opentelemetry-exporter-otlp>=1.20",
    "redis>=5.0",
    "sqlalchemy[asyncio]>=2.0",
    "asyncpg>=0.29",
    "networkx>=3.0",
    "fastapi>=0.109",
    "uvicorn>=0.27",
    "python-jose>=3.3",
    "passlib[bcrypt]>=1.7",
]

[project.optional-dependencies]
dev = ["pytest>=8.0", "pytest-asyncio>=0.23", "httpx", "ruff>=0.2"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
```

## go.mod
```
module github.com/norviq-dev/norviq

go 1.22

require (
	k8s.io/client-go v0.29.0
	k8s.io/apimachinery v0.29.0
	sigs.k8s.io/controller-runtime v0.17.0
	github.com/spf13/cobra v1.8.0
	go.uber.org/zap v1.27.0
)
```

## Makefile
```makefile
.PHONY: test lint build docker-build

test:
	pytest tests/ -v --tb=short

lint:
	ruff check norviq/ tests/

build:
	pip install -e .
	cd norviq/webhook && go build -o ../../bin/webhook .
	cd norviq/cli && go build -o ../../bin/norviq-cli .

docker-build:
	podman build -t norviq-engine -f Dockerfile.engine .
	podman build -t norviq-api -f Dockerfile.api .
	podman build -t norviq-sidecar -f Dockerfile.sidecar .
```

## .gitignore
```
__pycache__/
*.pyc
.venv/
*.egg-info/
dist/
build/
bin/
.env
.env.dev
*.log
node_modules/
ui/dist/
.cursor/
```

## README.md
```markdown
# Norviq

Runtime security platform for LLM agent tool calls on Kubernetes.

## What It Does

Norviq sits between LangGraph/LangChain agent reasoning loops and their tools. Every tool call is intercepted, evaluated against OPA/Rego policies scoped to Kubernetes workload identity (SPIFFE/SPIRE SVIDs), and either allowed, blocked, escalated, or audited.

## Quick Start

```bash
# Install
pip install -e ".[dev]"

# Run tests
make test

# Lint
make lint
```

## License

Apache 2.0
```

## Pseudo-logic
```
1. Create root project directory:
   cd ~/norviq (repo already cloned)

2. Create all Python package directories + __init__.py:
   mkdir -p norviq/{sdk/core,sdk/langchain,sdk/langgraph,sdk/client}
   mkdir -p norviq/{engine,sidecar}
   mkdir -p norviq/{api/routers,api/db/migrations,api/db/jobs}
   touch norviq/__init__.py
   touch norviq/sdk/__init__.py
   touch norviq/sdk/core/__init__.py
   touch norviq/sdk/langchain/__init__.py
   touch norviq/sdk/langgraph/__init__.py
   touch norviq/sdk/client/__init__.py
   touch norviq/engine/__init__.py
   touch norviq/sidecar/__init__.py
   touch norviq/api/__init__.py
   touch norviq/api/routers/__init__.py
   touch norviq/api/db/__init__.py

3. Create Go directories (no __init__.py):
   mkdir -p norviq/{webhook,cli/cmd}

4. Create UI directories:
   mkdir -p ui/src/{components,services,store}

5. Create infra + docs directories:
   mkdir -p helm/norviq/templates
   mkdir -p {crds,docs,architecture,registry}
   mkdir -p .cursor/rules
   mkdir -p scripts

6. Create test directories:
   mkdir -p tests/{sdk,engine,sidecar,api,integration}

7. Create pyproject.toml (content above)
8. Create go.mod (content above)
9. Create Makefile (content above)
10. Create .gitignore (content above)
11. Create README.md (content above)
12. Create .github/workflows/ci.yml

13. Verify structure:
    find . -name "__init__.py" | wc -l  # should be 11
    find . -type d | sort

14. Commit:
    git add -A
    git commit -m "chore(S001): project scaffold + dependencies"
```

## Definition of Done
- [ ] All directories created per Files section
- [ ] 11 __init__.py files exist
- [ ] pyproject.toml valid (pip install -e . works)
- [ ] go.mod valid (go mod tidy works)
- [ ] Makefile targets work (make lint, make test)
- [ ] .gitignore covers Python + Go + Node + env files
- [ ] README.md has project description + quick start
