Norviq has been deployed.

To access the UI:
  kubectl port-forward svc/norviq-ui 3000:80 -n {{ .Release.Namespace }}
  Then open: http://localhost:3000

FIRST LOGIN
{{- if .Values.auth.enabled }}
  Local login (default): open the console and sign in as
      username: {{ .Values.auth.adminUsername | default "admin" }}
{{- if eq (.Values.auth.adminPassword | default "norviq") "norviq" }}
  {{- /* Mirror templates/secret.yaml: "norviq" is a SENTINEL, not the password. When it is in effect the
       chart reuses the existing secret or generates randAlphaNum 20, so printing the sentinel here would
       hand every default install a credential that cannot work -- and auth.loginMaxAttempts retries
       would then trip the brute-force lockout (429). Print how to READ the real one instead. */}}
      password: auto-generated by the chart -- read it with:
          kubectl get secret norviq-secrets -n {{ .Release.Namespace }} \
            -o jsonpath='{.data.NRVQ_AUTH_ADMIN_PASSWORD}' | base64 -d ; echo
  (A strong random password is generated on first install and preserved across `helm upgrade`. Pin your
  own instead by setting auth.adminPassword.)
{{- else }}
      password: the auth.adminPassword you set
{{- end }}
  You will be prompted to change it on first login (must_change=true), so this credential is single-use.
{{- end }}

  SSO (recommended for teams): enable OIDC (see docs/onboarding-login.md), then the console also shows
  "Sign in with SSO". Your IdP group maps to a Norviq role + namespace (Entra / Okta / Auth0 / Keycloak).

  Automation/CI: mint a short-lived admin token in-cluster (no password, key never leaves the cluster):
      norviq login -n {{ .Release.Namespace }} --console-url http://localhost:3000
  It prints a sign-in link (or paste the printed token under the console login's "Advanced" section).

To access the API:
  kubectl port-forward svc/norviq-api 8080:{{ .Values.api.port }} -n {{ .Release.Namespace }}
  Then: curl http://localhost:8080/healthz

{{- if .Values.webhook.injection.enabled }}

Sidecar injection is ENABLED (mode: {{ .Values.webhook.injection.sidecarMode | default "proxy" }}). TLS was
auto-bootstrapped by the norviq-webhook-cert hook Job (self-signed cert -> norviq-webhook-tls secret ->
patched webhook caBundle).
  Opt a namespace in:  kubectl label ns <ns> norviq-injection=enabled
  Every NEW pod in that namespace gets the Norviq sidecar injected — no per-pod label needed.
  Opt a single pod OUT:  set the pod label  norviq-injection=disabled  (or annotation
                         norviq.io/skip-injection=true).
  Optionally set the agent class:  pod label  norviq.io/agent-class=<class>.
{{- else }}

Sidecar injection is DISABLED. Turn it on (turnkey, auto-TLS, no cert-manager needed):
  helm upgrade --install norviq ./helm/norviq --set webhook.injection.enabled=true
  kubectl label ns <ns> norviq-injection=enabled
{{- end }}

To check status:
  kubectl get pods -n {{ .Release.Namespace }}

To view logs:
  kubectl logs -f deployment/norviq-api -n {{ .Release.Namespace }}
  kubectl logs -f deployment/norviq-engine -n {{ .Release.Namespace }}

Production reminder:
  Override api.secretKey, redis.password, and postgresql.password before production deployment.
  Set values.policyQuotaNamespaces to include every tenant namespace (for example: default, chatbot-prod, analytics); quotas and baseline policies render from this list.
  A baseline high-priority policy is installed for EACH namespace in policyQuotaNamespaces (values.baselineClusterPolicy.*); keep it enabled to preserve fail-closed posture. NOTE: with baselineClusterPolicy.enabled=true, an EMPTY policyQuotaNamespaces now FAILS the install (it would otherwise render no baseline and silently drop fail-closed posture) — list your tenant namespaces, or set enabled=false to opt out explicitly.
  Bind ClusterRoles to your tenant/admin groups (this chart ships roles but not subject-specific bindings).
  Policies with permissive preset and/or enforcementMode=audit provide visibility only and do not enforce blocks.
