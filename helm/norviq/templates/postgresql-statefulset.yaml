{{- if and .Values.postgresql.enabled (not .Values.postgresql.ha.enabled) }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: norviq-postgresql
  namespace: {{ .Values.namespace }}
  labels:
    app: norviq-postgresql
    {{- include "norviq.labels" . | nindent 4 }}
spec:
  serviceName: norviq-postgresql
  replicas: {{ .Values.postgresql.replicas }}
  selector:
    matchLabels:
      app: norviq-postgresql
  template:
    metadata:
      labels:
        app: norviq-postgresql
    spec:
      containers:
        - name: postgresql
          image: "{{ .Values.images.postgresql.repository }}:{{ .Values.images.postgresql.tag }}"
          # Partial hardening only. This bundled single-node Postgres is a dev convenience —
          # values-prod.yaml replaces it with CloudNativePG (asserted by
          # test_prod_profile_drops_the_unhardened_bundled_datastores). runAsNonRoot / drop-ALL are
          # deliberately NOT set: the official postgres entrypoint starts as root to chown the data dir
          # and then drops privileges, so forcing them breaks startup. These two controls are free.
          securityContext:
            allowPrivilegeEscalation: false
            seccompProfile:
              type: RuntimeDefault
          ports:
            - containerPort: {{ .Values.postgresql.port }}
              name: postgres
          env:
            - name: POSTGRES_DB
              value: {{ .Values.postgresql.database }}
            - name: POSTGRES_USER
              value: {{ .Values.postgresql.username }}
            - name: POSTGRES_PASSWORD
              value: {{ .Values.postgresql.password }}
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          resources:
            {{- toYaml .Values.postgresql.resources | nindent 12 }}
          volumeMounts:
            - name: pg-data
              mountPath: /var/lib/postgresql/data
      volumes:
        - name: pg-data
          persistentVolumeClaim:
            claimName: norviq-postgresql-pvc
{{- end }}
