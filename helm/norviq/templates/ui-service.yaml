apiVersion: v1
kind: Service
metadata:
  name: norviq-ui
  namespace: {{ .Values.namespace }}
  labels:
    app: norviq-ui
    {{- include "norviq.labels" . | nindent 4 }}
spec:
  selector:
    app: norviq-ui
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
