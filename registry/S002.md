# S002 Code Registry

## 1) Feature
- ID: S002
- Name: Config (pydantic_settings)
- Scope: central typed config via NRVQ_ env vars

## 2) Files
- `norviq/config.py`
- `tests/test_config.py`
- `.env.example`
- `architecture/S002.mmd`
- `registry/S002.md`

## 3) Structure
- `NorviqSettings` class at `norviq/config.py:6` defines all 24 config fields with defaults.
- Module-level singleton `settings = NorviqSettings()` at `norviq/config.py:37` is the shared import target.

## 4) Methods
- `NorviqSettings` (class only; field-based settings model).
- Tests validate defaults, env overrides, type parsing, and singleton import path.

## 5) Config
- Env prefix: `NRVQ_` via `SettingsConfigDict(env_prefix="NRVQ_")`.
- `.env.example` documents all 24 supported variables and defaults.

## 6) Data Flow
- Environment -> `NorviqSettings` parsing/validation -> typed attributes -> `settings` singleton imports.

## 7) Dependencies
- `pydantic_settings.BaseSettings`
- `pydantic_settings.SettingsConfigDict`

## 8) Tests
- `test_settings_defaults`
- `test_settings_reads_env_vars`
- `test_settings_validates_types`
- `test_settings_singleton_import`

## 9) Upstream Table
- S003 imports `settings`.
- F001-F005 import `settings`.
- F006/F007/F008/F009 consume specific settings fields.

## 10) Error Code Map
- No NRVQ error codes in this feature.

## 11) Concurrency
- Immutable read-mostly config values; singleton is initialized at import time.

## 12) Debug Guide
- Missing override: verify `NRVQ_` prefix spelling.
- Type parse issue: verify env values are valid `int`/`float`.
- Unexpected default: ensure env var exists in process environment before import.
- Timeout case: N/A - config is read once at import with no network or disk I/O.
- Fallback case: N/A - config has no retry/fallback runtime path.
