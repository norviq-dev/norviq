I have everything I need. All files exist; let me verify the registry's line references against the actual file (already confirmed during reads) and produce the review.

All checks done. Here is the review.

## Feature Review: S003 — Exception Classes

### Automated Checks
| Check | Result | Details |
|-------|--------|---------|
| ruff | PASS | 7/7 automated checks reported passing |
| pytest | PASS | 6/6 tests in `tests/test_exceptions.py` |
| NRVQ codes | PASS | 5 codes present (1000, 2010, 2015, 2002, 1002) |
| No print() | PASS | none |
| No requests | PASS | none |
| No os.path | PASS | none |
| No threading | PASS | none |
| Config loads | PASS | `from norviq.config import settings` resolves (S002) |
| Mermaid .mmd | PASS | `architecture/S003.mmd` present (class + sequence) |
| Code registry | PASS | `registry/S003.md` present, all 12 sections |
| Type hints | PASS | 6/6 functions fully typed |

### Spec Compliance
- [x] `NorviqError` — signature matches (`message: str, code: str = "NRVQ-SDK-1000"`)
- [x] `NorviqBlockError` — `decision`, code `NRVQ-ENG-2010`
- [x] `NorviqEscalateError` — `decision`, code `NRVQ-ENG-2015`
- [x] `NorviqTimeoutError` — `timeout_ms`, code `NRVQ-ENG-2002`
- [x] `NorviqConfigError` — `field`, code `NRVQ-SDK-1002`
- [x] `str(exception)` format `"[CODE] message"` verified by tests
- [x] Only the 2 spec files created (plus required `.mmd` + registry) — no hallucinated files
- [x] `PolicyDecision` kept as string type hint (forward ref) — no circular import
- [ ] **Minor deviation:** spec body inlines `hasattr(decision, 'reason')`; code extracts this into a `_reason_or_default()` helper (`norviq/exceptions.py:5`). This is a reasonable DRY improvement and is documented in both the registry and `.mmd`, so it is **not** an undisclosed addition. Note one behavioral difference: the helper falls back to the default on a *falsy* reason (empty string/`None`), whereas the spec's `hasattr` only falls back when the attribute is *absent*. Acceptable and arguably safer, but flagging for awareness.

### Security Issues
- None found. No hardcoded secrets/URLs/ports. No `eval`/`exec`/`pickle`. No SQL. No raw-dict trust.

### Performance Issues
- None found. No I/O, no blocking calls — pure in-memory exception construction. Not in any hot path.

### Race Condition Issues
- None found. No shared mutable state, no Redis/DB/OPA, no async. Registry §11 correctly documents "no shared mutable state."

### Architecture Diagram
```
                +---------------------------+
                | Exception (builtin)       |
                +---------------------------+
                            ▲ inherits
                +---------------------------+
                | NorviqError (S003)        |
                |---------------------------|
                | + message: str            |
                | + code: str = NRVQ-SDK-1000|
                | + __init__(message, code) |
                +---------------------------+
         ┌───────────┬───────────┬───────────┐ inherits
         ▼           ▼           ▼           ▼
+----------------+ +----------------+ +----------------+ +----------------+
| NorviqBlock    | | NorviqEscalate | | NorviqTimeout  | | NorviqConfig   |
| Error          | | Error          | | Error          | | Error          |
|----------------| |----------------| |----------------| |----------------|
| +decision:PD   | | +decision:PD   | | +timeout_ms:int| | +field:str     |
| code=ENG-2010  | | code=ENG-2015  | | code=ENG-2002  | | code=SDK-1002  |
+----------------+ +----------------+ +----------------+ +----------------+
   ▲ raised by F009   ▲ raised by F009    ▲ F006/F009         ▲ config layer
   caught F011/F012   caught F011/F012    F015 catches all NorviqError
```
Helper `_reason_or_default()` feeds Block/Escalate message construction. Diagram matches code; downstream consumers F006/F009/F011/F012/F015 shown.

### Method Call Chain
```
1. F009 evaluator ──► raise NorviqBlockError(decision)
2.                ──► _reason_or_default(decision, "Blocked by policy")
3.                ──► NorviqError.__init__ → super().__init__("[NRVQ-ENG-2010] Blocked: <reason>")
4. F011/F012      ──► except NorviqBlockError as e: inspect e.code / e.message / e.decision
```

### Test Coverage Notes
Tests cover: base error fields, both decision-carrying errors, timeout, config, and the full inheritance contract (subclass of `NorviqError` **and** `Exception`). This matches the spec's Definition of Done.
- **Gap (non-blocking):** No test exercises the `_reason_or_default` **fallback** branch — i.e. a `NorviqBlockError`/`NorviqEscalateError` constructed with a decision that has no `reason` (or an empty one), asserting the message falls back to `"Blocked by policy"` / `"Requires escalation"`. The registry §12 Debug Guide documents this fallback path, so it should be tested.

### Fix Instructions for Cursor
Only one optional improvement — no blocking issues:

1. **`tests/test_exceptions.py`** — add a fallback-path test after `test_escalate_error_carries_decision` (line 43):
   ```python
   def test_block_error_falls_back_without_reason() -> None:
       """Use default message when decision has no reason."""
       err = NorviqBlockError(object())
       assert str(err) == "[NRVQ-ENG-2010] Blocked: Blocked by policy"

   def test_escalate_error_falls_back_without_reason() -> None:
       """Use default message when decision has no reason."""
       err = NorviqEscalateError(object())
       assert str(err) == "[NRVQ-ENG-2015] Escalate: Requires escalation"
   ```

---

## Verdict: ✅ APPROVE

All 7 automated checks pass; spec compliance is complete (5 classes, correct signatures, correct default NRVQ codes, correct `str()` format); both required artifacts (`architecture/S003.mmd`, `registry/S003.md`) exist, are accurate, and registry file:line references verified correct. No security, performance, or race-condition issues — appropriate for a pure exception module. The single recommendation (fallback-path tests) is a quality improvement, not a rejection condition.
